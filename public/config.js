window.__ASGARD_CONFIG__ = {
  apiOrigin: "",
  // Admin credentials for SINGLE_USER_MODE auto-login.
  // In production, set these via environment variables at build time or
  // override this file in the Docker image entrypoint.
  adminEmail: "admin@asgard.dev",
  adminPassword: "password",
};
