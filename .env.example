VITE_ASGARD_API_ORIGIN=http://localhost:8000

# Admin credentials for SINGLE_USER_MODE auto-login.
# These are baked into the build at compile time. For runtime override,
# edit public/config.js (or mount a custom config.js in Docker).
# VITE_ADMIN_EMAIL=admin@asgard.dev
# VITE_ADMIN_PASSWORD=change-me
